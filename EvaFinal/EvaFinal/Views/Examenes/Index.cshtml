@model IEnumerable<EvaFinal.Models.Examen>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.NPreguntas)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Fecha)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NPreguntas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ExamenId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ExamenId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ExamenId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
